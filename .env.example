# Configurazione ERMES per NAS + GitHub con Polling Automatico
# Copia questo file come .env e modifica i valori secondo le tue esigenze

# ============================================
# Configurazione GitHub
# ============================================

# Repository GitHub (formato: username/repo-name)
GITHUB_REPO=Paluello/ERMES

# Branch da monitorare per aggiornamenti
GITHUB_BRANCH=main

# Token GitHub (opzionale, solo per repo privati)
# Lascia vuoto se il repo è pubblico
# Per creare un token: https://github.com/settings/tokens
GITHUB_TOKEN=

# ============================================
# Aggiornamento Automatico (Polling)
# ============================================

# Abilita aggiornamento automatico via polling (consigliato per VPN Tailscale)
# Il sistema controllerà GitHub ogni X minuti per nuovi commit
GITHUB_AUTO_UPDATE_ENABLED=true

# Intervallo di polling in minuti (default: 5)
# Controlla GitHub ogni 5 minuti per nuovi commit
GITHUB_AUTO_UPDATE_INTERVAL_MINUTES=5

# ============================================
# Configurazione Docker (NAS)
# ============================================

# Directory del progetto sul NAS (solo se diversa da quella corrente)
# Esempio per Synology: /volume1/docker/ERMES
# Lascia vuoto per auto-detect
PROJECT_DIR=

# Path di mount nel container (default: /workspace)
# Lascia vuoto per auto-detect
PROJECT_DIR_MOUNT=

# ============================================
# Configurazione Applicazione
# ============================================

# Precisione GPS (standard o rtk)
GPS_PRECISION=standard

# Modello YOLO da usare (yolov8n.pt = nano, più leggero per NAS)
YOLO_MODEL=yolov8n.pt

# Soglia di confidenza YOLO (0.0 - 1.0)
YOLO_CONF_THRESHOLD=0.6

# Numero massimo di oggetti tracciati simultaneamente
MAX_TRACKED_OBJECTS=10

# FPS video (ridotto per NAS meno potenti)
VIDEO_FPS=15

# Host API (0.0.0.0 = accetta connessioni da qualsiasi IP)
API_HOST=0.0.0.0

# Porta API
API_PORT=8000

# ============================================
# Note
# ============================================
# 
# Per setup completo, vedi: SETUP_NAS_POLLING.md
# 
# Il polling automatico è la soluzione consigliata quando:
# - Usi VPN Tailscale (webhook non accessibili da internet)
# - Vuoi aggiornamenti automatici senza intervento manuale
# 
# Il sistema controllerà GitHub ogni X minuti e aggiornerà
# automaticamente il codice quando rileva nuovi commit.
